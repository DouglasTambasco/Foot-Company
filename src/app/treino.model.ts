export interface Treino {
    horario: string;     // Tipo: string, ex: "15h"
    local: string;       // Tipo: string, ex: "Av. VTNC"
    treinador: string;   // Tipo: string, ex: "MEU PAU" ou o UID do treinador
  }
  