import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TreinoComBolaPage } from './treino-com-bola.page';

describe('TreinoComBolaPage', () => {
  let component: TreinoComBolaPage;
  let fixture: ComponentFixture<TreinoComBolaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TreinoComBolaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
